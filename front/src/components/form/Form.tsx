import styles from "./Form.module.css";

const Form = () => {
  return (
    <div className={styles.form}>
      <form>
        <button>Buscar Pokemon</button>
      </form>
    </div>
  );
};

export default Form;
